// Teste: função do usuário (só JS)
function soma(a, b) {
    return a + b;
}

let resultado = soma(3, 7);
print(resultado);

function fatorial(n) {
    if (n <= 1) {
        return 1;
    }
    return n * fatorial(n - 1);
}

print(fatorial(5));
